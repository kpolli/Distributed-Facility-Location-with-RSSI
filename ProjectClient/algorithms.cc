void SetB (message_t *msg)
{
	nx_uint16_t nB = 6;
	nx_uint16_t nD = nB * 3;
	for (nx_uint16_t i = 0; i < nB; i++){
		for (nx_uint16_t j = 0; i < nD;) {
		

	nx_uint16_t nB = 6;
	nx_uint16_t nD = nB * 2; 
numB[nB] = nB;
	for (nx_uint16_t i = 0; i < nB; i++){
		for (nx_uint16_t j = 1; i < nD;) {
	numB[i].m_numClient = i;
	numB[i}.m_clientdists = j;
	j = j * 2;
}}


	

